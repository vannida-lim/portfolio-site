import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`

`

export const Work = () => {
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default Work;
